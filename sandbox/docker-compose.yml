version: '3.8'

# define the main application and database services for the local environment
services:
  web:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: self_healing_app
    restart: unless-stopped
    environment:
      - DB_HOST=db
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=secret
      - POSTGRES_DB=app_db

  db:
    image: postgres:13-alpine
    container_name: self_healing_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: app_db
    # publish on an ephemeral host port so parallel runners never collide
    ports:
      - "5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d app_db"]
      interval: 3s
      timeout: 3s
      retries: 5
